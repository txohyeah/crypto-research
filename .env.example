# 复制为 .env 后使用（.gitignore 已排除，绝不入库）。
# 飞书推送走 qwenpaw channels send（复用实例已绑定的 feishu channel），无需 webhook。
PROXY_URL=http://127.0.0.1:7890

# 飞书推送默认接收人（btc/scripts/feishu_send_file.py 等脚本读取；
# 2026-09-16 起不再硬编码在源码里，仓库是公开仓库）
FEISHU_OPEN_ID=
